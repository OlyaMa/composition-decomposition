import { ValueProps } from "@/app/page";

/**
 * Компонент слогана под формой поиска.
 *
 * @param {ValueProps} props Передаем пропсы для его формирования.
 * @returns {JSX.Element} Компонент слогана под формой поиска.
 **/
export const Slogan = (props: ValueProps) => {
  return (
    <div>
      Слоган<span>тест</span>
    </div>
  );
};
