import { ValueProps } from "@/app/page";

/**
 * Компонент картинки.
 *
 * @param {ValueProps} props Передаем нужную информацию в блок.
 * @returns {JSX.Element} Компонент картинки.
 **/
export const Img = (props: ValueProps) => {
  return <img src="#" alt="картинка" />;
};
