import { ValueProps } from "@/app/page";

/**
 * Компонент крупной синей ссылки.
 *
 * @param {ValueProps} props Передаем нужную информацию в блок.
 * @returns {JSX.Element} Компонент крупной синей ссылки.
 **/
export const BigLink = (props: ValueProps) => {
  return (
    <a href="#">
      <h3></h3>
    </a>
  );
};
