import { ValueProps } from "@/app/page";

/**
 * Компонент разницы курса валюты.
 *
 * @param {ValueProps} props Передаем нужную информацию в блок.
 * @returns {JSX.Element} Компонент разницы курса валюты..
 **/
export const Diff = (props: ValueProps) => {
  return <div>+10%</div>;
};
