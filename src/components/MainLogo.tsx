import { ValueProps } from "@/app/page";

/**
 * Компонент с лого Поисковика.
 *
 * @param {ValueProps} props Передаем дочерние компоненты.
 * @returns {JSX.Element} Компонент с лого Поисковика.
 **/
export const MainLogo = (props: ValueProps) => {
  return <img src="#" alt="картинка" />;
};
