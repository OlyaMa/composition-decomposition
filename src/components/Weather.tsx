import { ValueProps } from "@/app/page";

/**
 * Компонент информации с прогнозом погоды.
 *
 * @param {ValueProps} props Передаем пропсы для его формирования.
 * @returns {JSX.Element} Компонент информации с прогнозом погоды.
 **/
export const Weather = (props: ValueProps) => {
  return <div>Компонент погоды</div>;
};
