import { ValueProps } from "@/app/page";

/**
 * Компонент текущего времени.
 *
 * @param {ValueProps} props Передаем нужную информацию в блок.
 * @returns {JSX.Element} Компонент текущего времени.
 **/
export const CurrentTime = (props: ValueProps) => {
  return <div>Time</div>;
};
