import { ValueProps } from "@/app/page";

/**
 * Компонент раздела с кнопкой эфира.
 *
 * @param {ValueProps} props Передаем пропсы для его формирования.
 * @returns {JSX.Element} Компонент раздела с кнопкой эфира.
 **/
export const EfirButton = (props: ValueProps) => {
  return <button>EfirButton</button>;
};
